# 运行环境: development | production
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8000

# 数据源模式: mock | udp | zmq
DATA_SOURCE=mock

# UDP 配置（DATA_SOURCE=udp 时生效）
UDP_HOST=0.0.0.0
UDP_PORT=9000

# ZMQ Broker 地址
ZMQ_BROKER_FRONTEND=tcp://127.0.0.1:5555
ZMQ_BROKER_BACKEND=tcp://127.0.0.1:5556
